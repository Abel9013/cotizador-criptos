import styled from '@emotion/styled'

// Se crea por fuera del componente
const Heading = styled.h1`
  font-family: 'Lato', sans-serif;
  color: #FFF;
`

function App() {  

  return (
   <Heading>Desde App</Heading>
  )
}

export default App
